services:
    db:
        image: postgres
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgress
            - POSTGRES_DB=postgres
        ports:
            - "5433:5432"
        volumes:
            - ecomdb_vol:/var/lib/postgresql

volumes:
    ecomdb_vol:
        external: true